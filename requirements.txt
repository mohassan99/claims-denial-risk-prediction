pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
shap>=0.44
matplotlib>=3.7
seaborn>=0.12
pyarrow>=14.0
sentence-transformers>=2.2
faiss-cpu>=1.7
jupyter
python-dotenv
black
ruff
pre-commit

# Firth's penalized (bias-reduced) logistic regression -- added 2026-09-23
# for src/fit_chow_test.py's --method firth option, the standard fix for
# the separation confirmed in diagnose_separation.py /
# verify_provider_outlier_mechanism.py (6 HCPCS codes with an exact 0.000
# population-wide denial rate). See fit_chow_test.py's _fit_logit_firth
# docstring for why fit_intercept=False and wald=True are required, and
# for the asymptotic caveat that comes with using it under true separation.
firthlogist>=0.5.0
