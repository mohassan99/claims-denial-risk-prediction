pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
shap>=0.44
matplotlib>=3.7
seaborn>=0.12
pyarrow>=14.0
sentence-transformers>=2.2
faiss-cpu>=1.7
jupyter
python-dotenv
black
ruff
pre-commit

# Used since Phase 2's Chow-test work (build_chow_design_matrix.py,
# fit_chow_test.py, diagnose_separation.py) but never actually pinned here
# until now -- caught via a local uncommitted requirements.txt edit
# (2026-09-23), not added in advance.
statsmodels==0.15.0

# Firth's penalized (bias-reduced) logistic regression -- added 2026-09-23
# for src/fit_chow_test.py's --method firth option, the standard fix for
# the separation confirmed in diagnose_separation.py /
# verify_provider_outlier_mechanism.py (6 HCPCS codes with an exact 0.000
# population-wide denial rate). CORRECTED same day: originally pinned as
# `firthlogist`, which turned out to be archived by its author (Dec 2025)
# and pulled from PyPI entirely -- switched to `firthmodels`, the
# author's own successor package, confirmed installable and verified
# against its actual API (see fit_chow_test.py's _fit_logit_firth
# docstring) before writing any code against it.
firthmodels>=0.8.2
